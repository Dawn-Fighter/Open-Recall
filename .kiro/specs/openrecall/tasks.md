# Implementation Plan: OpenRecall

## Overview

Convert the existing single-incident `incident-memory-agent/` project into the OpenRecall queue-driven alert triage co-pilot. The plan is split into two phases:

- **Phase 1 (tasks 1–17):** the original implementation under a Streamlit cockpit. All seventeen tasks are complete and the package code, property tests, smoke tests, and seed data continue to satisfy R1–R15.
- **Phase 2 (task 18, end of file):** the post-launch Streamlit cutover. The Streamlit cockpit (`app.py`, `.streamlit/`) was retired in favor of a FastAPI backend (`api.py`) plus a Next.js 16 cockpit (`frontend/`). The package modules under `incident_agent/` are unchanged; only the entry point, the cockpit, and the docs/steering moved.

Tasks 1–17 are preserved verbatim as the historical implementation record so every R/P trace remains auditable. Where they reference `app.py`, `streamlit run`, or the Altair chart, those references describe the state of the system at the time the task was completed; the current state is documented in Phase 2 and in `design.md` after its post-cutover update.

All paths in this plan are relative to `incident-memory-agent/`. The current implementation language is **Python 3.11+** for the FastAPI backend (FastAPI, Pydantic v2, hindsight-client, cascadeflow[groq], Hypothesis) plus **Node.js 20+** for the Next.js cockpit (next 16, react 19, tailwindcss 4, shadcn 4).

The acronyms used below: P# = correctness Property # from `requirements.md` (also restated in `design.md` § Correctness Properties); R#.# = requirement clause number from `requirements.md`.

## Tasks

- [x] 1. Repo hygiene and secrets purge
  - [x] 1.1 Audit the working tree for committed secrets matching `gsk_[A-Za-z0-9_]{20,}` and `hsk_[A-Za-z0-9_]{20,}` (R12.4); remove any live values from `.env` and any tracked file, leaving only placeholders. Rotate the previously committed Groq key out-of-band and document the rotation in `SECURITY.md`. (R12.1, R12.4, design §Configuration / Environment Variables)
  - [x] 1.2 Update `.gitignore` to ensure `.env`, `.streamlit/secrets.toml`, and `data/local_memory.json` are ignored; verify the entries are present (some already are). (R12.2)
  - [x] 1.3 Replace `.env.example` with the OpenRecall env block per design Configuration table: `HINDSIGHT_BASE_URL=https://api.hindsight.vectorize.io`, `HINDSIGHT_API_KEY=hsk_your_key_here`, `HINDSIGHT_BANK_ID=openrecall`, `GROQ_API_KEY=gsk_your_key_here`, `CASCADEFLOW_MODE=observe`, `CASCADEFLOW_LIVE_GROQ=false`, `CASCADEFLOW_RUN_BUDGET_USD=0.02`, `CASCADEFLOW_CHEAP_MODEL=qwen/qwen3-32b`, `CASCADEFLOW_STRONG_MODEL=openai/gpt-oss-120b`, `OPENRECALL_PBT_SEED=20260101`, `OPENRECALL_BATCH_SIZE_LIMIT=200`, `HINDSIGHT_SEED_LIMIT=0`, `HINDSIGHT_SEED_DELAY_SECONDS=0`. Drop the open-source local Hindsight Docker block (`HINDSIGHT_API_LLM_*`, `HINDSIGHT_API_CONSOLIDATION_*`, `HINDSIGHT_API_RETAIN_*`) per design §Configuration removed. (R12.3, design §Configuration / Environment Variables)
  - [x] 1.4 Document the new env vars and the cloud/fallback distinction in `SECURITY.md` and a new short section in `README.md` under "Configuration" naming each variable, its default, and which component reads it. (R12.3, design §Configuration table)

- [x] 2. Project rename and dependency bump
  - [x] 2.1 Update `pyproject.toml`: rename `[project] name` from `incident-memory-agent` to `openrecall`, bump `version` to `0.2.0`, leave `requires-python`, `license`, runtime `dependencies` list intact. Do NOT rename the `incident_agent/` Python package directory (design explicitly preserves the import path). (design §Removed/renamed)
  - [x] 2.2 Add `[project.optional-dependencies] dev = ["hypothesis>=6.108", "pytest>=8.0", "altair>=5.2"]` to `pyproject.toml`. (design §File-by-file Plan, R15.1)
  - [x] 2.3 Append `hypothesis>=6.108`, `pytest>=8.0`, `altair>=5.2` to `requirements.txt` so a fresh `pip install -r requirements.txt` brings PBT and the cost-curve chart deps. (R14.4, R15.1, design §File-by-file Plan)
  - [x] 2.4 Run `python -m compileall app.py incident_agent seed_memory.py scripts/smoke_test.py` to confirm the rename did not break imports; the package directory stays `incident_agent/`. (R14.1)

- [x] 3. Pydantic models — extensions and new types in `incident_agent/models.py`
  - [x] 3.1 Add `Alert` BaseModel: `raw_alert: str`, `title: str | None = None`, `submitted_at: str = Field(default_factory=utc_now)`. (R2.1, design §Data Models)
  - [x] 3.2 Add `AlertFingerprint` BaseModel with the six fields (`error_class`, `service_role`, `dependency_pattern`, `signal_shape`, `attack_pattern`, `environment`) all `str` defaulting to empty string and `model_config = ConfigDict(frozen=True)` so the model is hashable for use as dedupe key. (R3.1, R3.6, design §Data Models)
  - [x] 3.3 Add `TriageDecision = Literal["false_positive", "duplicate", "known_benign", "real", "escalated"]` next to the existing `IncidentType` literal. (R5.1, design §Data Models)
  - [x] 3.4 Add `TriageResult` BaseModel with `proposed_decision: TriageDecision`, `triage_confidence: float` (0..1), `supporting_matches: list[MemoryMatch]`, `consistent_decision_count: int = 0`, `requires_human_approval: bool = False`, `escalation_reason: str = ""`. (R5.2, R5.3, R5.5, design §Data Models)
  - [x] 3.5 Add `CostCurvePoint` BaseModel with `alert_index: int`, `cost_usd: float`, `baseline_cost_usd: float`, `cumulative_cost_usd: float`, `cumulative_baseline_usd: float`, `batch_id: int = 0`. (R9.1, R9.5, design §Data Models)
  - [x] 3.6 Add `AuditTraceEntry` BaseModel with `step: str`, `model: str`, `route_reason: str`, `memory_hit_count: int`, `proposed_decision: TriageDecision | None = None`, `escalation_reason: str | None = None`, `live_model_call: bool`, `llm_skipped: bool = False`, `cost_usd: float`, `baseline_cost_usd: float`, `savings_usd: float`. (R10.1, R10.3, design §Data Models)
  - [x] 3.7 Extend `RouteTrace` with the four new optional fields and `budget_exhausted`: `triage_decision_proposed: TriageDecision | None = None`, `memory_match_score: float | None = None`, `decision_consistency: float | None = None`, `llm_skipped: bool = False`, `budget_exhausted: bool = False`. All defaults preserve backward compatibility per design §Backward Compatibility Plan. (R10.4, R6.6, design §Data Models)
  - [x] 3.8 Extend `AnalysisResult` with the five new optional fields (all with safe defaults): `alert_fingerprint: AlertFingerprint | None = None`, `triage_result: TriageResult | None = None`, `cost_curve_point: CostCurvePoint | None = None`, `dead_ends: list[str] = Field(default_factory=list)`, `audit_trace: list[AuditTraceEntry] = Field(default_factory=list)`. (R1.1, R1.4, R7.1, R10.1, design §Data Models, §Backward Compatibility Plan)
  - [x] 3.9 Update `incident_agent/__init__.py` to re-export the new public names (`Alert`, `AlertFingerprint`, `TriageDecision`, `TriageResult`, `CostCurvePoint`, `AuditTraceEntry`). (design §File-by-file Plan)
  - [x]* 3.10 Add `tests/test_signatures.py` (lightweight example test, not Hypothesis) asserting `IncidentWorkflow.analyze` signature is `(self, raw_alert: str) -> AnalysisResult` and that `AnalysisResult` exposes every new field name. (R1.2, R14.1, design §Example/Smoke Tests)

- [x] 4. Fingerprint module — `incident_agent/fingerprint.py`
  - [x] 4.1 Create `incident_agent/fingerprint.py` and move `infer_error_type`, `pick_known_service`, `infer_dependency` from `workflow.py` into it; re-import them back into `workflow.py` to preserve every existing caller (R14.5, design §Pseudocode "Fingerprint generation").
  - [x] 4.2 Implement `match_first(text, patterns, default)` helper and `regex_fallback(raw_alert) -> AlertFingerprint` per design pseudocode using `infer_error_type`, `pick_known_service`, `infer_dependency`, plus signal-shape and attack-pattern keyword lists from design (`5xx`, `4xx`, `timeout`, `crashloop`, `oom`, `latency`, `p95`; `sql injection`, `credential stuffing`, `jwt`, `waf`, `attack`, `csrf`, `xss`). (R3.3, R3.4, design §Pseudocode "Fingerprint generation")
  - [x] 4.3 Implement `format_fingerprint(fp) -> str` emitting the canonical form `error_class=<v>; service_role=<v>; dependency_pattern=<v>; signal_shape=<v>; attack_pattern=<v>; environment=<v>` with empty fields rendered as the literal `none`. Field order is fixed. (R3.6, design §Key Public Interfaces)
  - [x] 4.4 Implement `parse_fingerprint(text) -> AlertFingerprint` as a strict regex over the same six-key shape; treat the literal `none` as empty string; raise `ValueError` on unknown keys or missing keys. The pair must satisfy P2 (round-trip). (R3.7, design §Key Public Interfaces)
  - [x] 4.5 Implement `FingerprintGenerator` class with `__init__(self, router: CascadeFlowRouter)` and `generate(raw_alert) -> tuple[AlertFingerprint, RouteTrace]` per design pseudocode: when `router.live_groq` and `router.api_key`, call `router.fingerprint_with_trace`, fill missing fields from `regex_fallback`, validate via Pydantic; otherwise produce `regex_fallback` and a synthetic `step="alert fingerprint"` RouteTrace with `live_model_call=False`, `cost_usd=0.0`, `baseline_cost_usd=estimate_cost(raw_alert, router.strong_model)`. (R3.2, R3.3, R3.5, design §Pseudocode "Fingerprint generation")
  - [x] 4.6 Implement `fill_missing_with_regex(response, raw_alert)` helper used by `generate` to merge the cheap-model JSON with regex fallback for any missing/empty fingerprint field. (design §Pseudocode "Fingerprint generation")

- [x] 5. Triage engine — `incident_agent/triage.py`
  - [x] 5.1 Create `incident_agent/triage.py` with the three threshold constants `STRONG_MATCH_THRESHOLD: Final[float] = 0.85`, `DECISION_CONSISTENCY_THRESHOLD: Final[float] = 0.9`, `BYPASS_CONFIDENCE_THRESHOLD: Final[float] = 0.85` so they can be tuned in one place and referenced verbatim by the property tests. (R5.3, R6.1, design §Triage Decision Flowchart)
  - [x] 5.2 Implement `TriageEngine.decision_consistency(strong_matches) -> tuple[TriageDecision | None, float, int]` exactly per design pseudocode: sort strong matches by score desc (deterministic insertion order for `Counter` tie-break), `Counter` on stored decisions, return `(dominant, n_dominant/n_strong, n_dominant)`. (R5.5, design §Pseudocode "Decision_Consistency algorithm")
  - [x] 5.3 Implement `TriageEngine.confidence(consistency, n_strong) -> float` clamped per Property 5 bound: `min(c, max(c * (n / (n + 1)), c * (1 - 1 / (n + 2))))`. Always returns a value in `[0, 1]` for non-negative `c`. (R5.2, P4, P5, design §Pseudocode `clamp_confidence`)
  - [x] 5.4 Implement `TriageEngine.triage(fp, matches) -> TriageResult` per design pseudocode: filter Strong_Matches at `>= STRONG_MATCH_THRESHOLD`; on empty Strong_Matches return `escalated` (P11); when `fp.attack_pattern` is non-empty AND no Strong_Match, return `escalated` (P12); when Strong_Matches lack stored decisions, return `escalated`; when `consistency < DECISION_CONSISTENCY_THRESHOLD`, return `escalated`; otherwise return dominant decision with `requires_human_approval=True`. (R5.3, R5.4, R5.6, P3, P4, P5, P11, P12, design §Pseudocode "Auto-triage decision algorithm")
  - [x] 5.5 Add the security-novel kill switch path: when `fp.attack_pattern != ""` AND Strong_Match exists AND consistency is high, still return the dominant decision but with `requires_human_approval=True` and `escalation_reason="security-novel: human approval required"`. (R5.6, design §Pseudocode "Auto-triage decision algorithm")

- [x] 6. Cost curve tracker — `incident_agent/cost_curve.py`
  - [x] 6.1 Create `incident_agent/cost_curve.py` with `CostCurveTracker` class per design pseudocode: `__init__` initializes `_points: list[CostCurvePoint]`, `_cum_cost`, `_cum_baseline`, `_batch_id=0`. (R9.1, design §Pseudocode "Cost curve series with bypass case")
  - [x] 6.2 Implement `record(alert_index, cost_usd, baseline_cost_usd) -> CostCurvePoint`: increment cumulatives, append a new `CostCurvePoint`, return it. The `cost_usd` may be `0.0` on bypass; the `baseline_cost_usd` is preserved from the strong-model estimate so Property 9 holds. (R9.1, R9.4, P9, design §Pseudocode)
  - [x] 6.3 Implement `series() -> list[CostCurvePoint]` returning a copy of the internal list. (R9.5, design §Pseudocode)
  - [x] 6.4 Implement `mean_cost_per_alert(batch_id=None) -> float` filtering by `batch_id` when given, returning `0.0` on empty filter. (R9.5, P8, design §Pseudocode)
  - [x] 6.5 Implement `savings() -> tuple[float, float, float]` returning `(cumulative_cost, cumulative_baseline, percent_saved)`. (R9.3, design §Pseudocode)
  - [x] 6.6 Implement `begin_batch()` that increments `_batch_id` so callers can mark batch boundaries between Streamlit submissions. (R9.5, design §Pseudocode)

- [x] 7. Audit trace recorder — `incident_agent/audit.py`
  - [x] 7.1 Create `incident_agent/audit.py` with `AuditTraceRecorder` class containing a single `@staticmethod build(fingerprint, matches, triage_result, route_trace, cost_point) -> list[AuditTraceEntry]` that produces exactly one entry per `RouteTrace` to satisfy P15 (`len(audit) == len(route_trace)`). (R10.1, R10.3, P15, design §Key Public Interfaces, §Components)
  - [x] 7.2 Per RouteTrace populate every required `AuditTraceEntry` field (`step`, `model`, `route_reason`, `memory_hit_count = len(matches)`, `proposed_decision = triage_result.proposed_decision`, `escalation_reason = triage_result.escalation_reason`, `live_model_call`, `llm_skipped`, `cost_usd = trace.estimated_cost_usd`, `baseline_cost_usd = trace.strong_model_baseline_cost_usd`, `savings_usd = trace.savings_vs_strong_usd`) so no None leaks through for required fields per P15. (R10.1, R10.3, P15)

- [x] 8. Memory adapter — Hindsight Cloud, fingerprint recall, idempotent retain (`incident_agent/memory.py`)
  - [x] 8.1 Update `IncidentMemory.__init__` defaults: `HINDSIGHT_BASE_URL` default to `https://api.hindsight.vectorize.io`, `HINDSIGHT_BANK_ID` default to `openrecall`. Read `HINDSIGHT_API_KEY` from environment only. (R11.1, R12.1, design §Pseudocode "Hindsight Cloud client init")
  - [x] 8.2 Add module-level helper `_build_hindsight_client(base_url, api_key)` that probes `Hindsight(base_url=..., api_key=...)` first and falls back to header injection on the underlying httpx client if the SDK raises `TypeError`. (design §Hindsight Cloud Client Wiring)
  - [x] 8.3 Rewrite `_init_hindsight` per design pseudocode: when `api_key` is unset go straight to fallback with status `"fallback mode: HINDSIGHT_API_KEY unset"`; otherwise `httpx.head` health probe with 2s timeout and `Authorization: Bearer ${api_key}`; treat 401/403 as host-up; flip `fallback_mode=True` on connection error or 5xx with status string carrying the exception class name. (R11.1, R11.2, R11.3, R12.5, design §Pseudocode "Hindsight Cloud client init")
  - [x] 8.4 Add `recall_by_fingerprint(fingerprint, limit=5) -> list[MemoryMatch]` querying Hindsight Cloud first when client is initialized; on any exception or empty result, fall back to local. Construct the recall query from `format_fingerprint(fingerprint)` plus the same fingerprint fields concatenated for the local-recall scoring path. (R4.1, R4.2, R4.4, R4.6, design §Components "Memory_Adapter")
  - [x] 8.5 Add `recall_by_decision(fingerprint, decision, limit=5) -> list[MemoryMatch]` that calls `recall_by_fingerprint` then post-filters on `match.metadata.get("triage_decision") == decision`. (R4.3)
  - [x] 8.6 Extend `retain` to accept the new keyword-only args `*, fingerprint=None, decision=None, dead_ends=None, analyst_id=None, business_impact_minutes=None`; preserve the legacy positional/keyword `(content, context, metadata)` shape so `seed()` and the existing learning-loop button keep working. (R8.1, R8.2, design §Backward Compatibility Plan)
  - [x] 8.7 Add an in-process dedupe index `self._dedupe_index: set[tuple[str, str, str]]` keyed on `(format_fingerprint(fingerprint), decision_or_empty, sha256(content))` and short-circuit retain calls whose key is already present, returning `"skipped duplicate"`. (R8.3, P7, design §Pseudocode "Retain idempotence")
  - [x] 8.8 On retain success, write the new metadata block (`fingerprint_canonical`, `triage_decision`, `dead_ends`, `analyst_id`, `business_impact_minutes`) to the Hindsight call and to the local JSON fallback record. Local-fallback dedupe additionally re-checks the on-disk file because `local_memory.json` may persist across processes. (R8.4, R8.5, R7.4, design §Pseudocode "Retain idempotence")
  - [x] 8.9 Update `_load_local_retained_memory` and `_local_recall` so the retained metadata (`triage_decision`, `dead_ends`, `fingerprint_canonical`, `analyst_id`, `business_impact_minutes`) is preserved on the returned `MemoryMatch.metadata` dict for downstream rendering and for `recall_by_decision`. (R7.5, R8.5, design §Components "Memory_Adapter")

- [x] 9. Router extensions — `incident_agent/router.py`
  - [x] 9.1 Add `fingerprint_with_trace(raw_alert) -> tuple[dict | None, RouteTrace]` that builds the fingerprint extraction prompt (asking for strict JSON with the six fingerprint keys) and reuses `_call_model` with `step="alert fingerprint"`, `model=self.cheap_model`, `escalated=False`. Returns `(json_dict_or_None, trace)`. (R3.2, R3.5, R10.1, design §Key Public Interfaces)
  - [x] 9.2 Add `triage_with_trace(fp, triage_result, matches) -> RouteTrace` that builds a synthetic memory-bypass RouteTrace per design: `step="auto-triage bypass"`, `model="memory-bypass"`, `route_reason=f"memory consistent across {n} prior decisions: {titles}"`, `confidence=triage_result.triage_confidence`, `latency_ms=0.0`, `estimated_cost_usd=0.0`, `strong_model_baseline_cost_usd` computed from the strong-model RCA prompt baseline, `savings_vs_strong_usd=baseline`, `escalated=False`, `live_model_call=False`, `llm_skipped=True`, plus the new fields `triage_decision_proposed`, `memory_match_score`, `decision_consistency`. (R6.1, R6.5, R10.3, P6, P15, design §Key Public Interfaces, §Pseudocode)
  - [x] 9.3 Add cumulative live-cost tracking on `CascadeFlowRouter` (`self._cum_live_cost: float = 0.0`) updated inside `_call_model` whenever a live call succeeds, and `self._budget_exhausted: bool = False` flipped to True once `_cum_live_cost + estimated_cost_for_next_call > self.run_budget`. (R6.6, P14, design §Error Handling "Budget exhaustion")
  - [x] 9.4 When `_budget_exhausted` is True, force `_call_model` to skip the live HTTP call, emit `live_model_call=False`, `budget_exhausted=True`, `route_reason="run budget exhausted; deterministic fallback"`, and let the in-flight call complete per the property's "final in-flight call" allowance. (R6.6, P14, design §Error Handling)
  - [x] 9.5 Make `rca_with_trace` accept and surface a `dead_ends: list[str]` argument so the strong-model prompt for `proposed_decision in {real, escalated}` can include "Skip these dead ends:" plus the deduped list per design. (R6.3, R7.1, design §Components, §Triage Decision Flowchart)

- [x] 10. Workflow orchestrator — `analyze_queue` and the new pipeline (`incident_agent/workflow.py`)
  - [x] 10.1 Inject `FingerprintGenerator`, `TriageEngine`, `CostCurveTracker`, `AuditTraceRecorder` into `IncidentWorkflow.__init__`. The `CostCurveTracker` should be passed in (so Streamlit can cache it across reruns); the others can be constructed locally. (design §Components, §Streamlit State Model)
  - [x] 10.2 Refactor `analyze(raw_alert)` to: (a) build incident object + classification (existing path), (b) generate fingerprint via `FingerprintGenerator.generate`, (c) call `memory.recall_by_fingerprint(fp)` instead of free-text recall when fingerprint is non-empty, (d) build `TriageResult` via `TriageEngine.triage`, (e) on bypass append a `triage_with_trace` synthetic RouteTrace and skip the strong-model call, (f) on `real`/`escalated` call `rca_with_trace` with the deduped `dead_ends` list, (g) record one CostCurvePoint via `CostCurveTracker.record`, (h) build `audit_trace` via `AuditTraceRecorder.build`. Preserve the existing `analyze(raw_alert: str) -> AnalysisResult` signature exactly. (R1.1, R1.2, R1.4, R6.1, R6.2, R6.3, R6.5, R10.1, P6, P15, design §Components, §Primary Sequence)
  - [x] 10.3 Populate the new optional `AnalysisResult` fields (`alert_fingerprint`, `triage_result`, `cost_curve_point`, `dead_ends`, `audit_trace`) on every analyze return so single-alert and queue paths share the same shape. The `dead_ends` field is the union of `metadata.dead_ends` across returned MemoryMatch records, deduped, first-seen-wins. (R1.4, R7.1, design §Data Models)
  - [x] 10.4 Update `_effective_confidence` and `_deterministic_rca` to consume the new triage path: when `TriageResult.proposed_decision == "escalated"` keep deterministic RCA + memory_match score; on bypass skip both deterministic and live RCA and use only the cached "matches[0]" reasoning so the deterministic RCA pipeline still produces verification commands. (R1.3, R6.1, R6.2)
  - [x] 10.5 Implement `analyze_queue(alerts: list[Alert]) -> list[AnalysisResult]` as a deterministic sequential loop: call `cost_tracker.begin_batch()`, then `analyze(alert.raw_alert)` per entry incrementing an internal `alert_index`. Retain visibility from N to N+1 is observable because each `retain` call updates the in-memory dedupe index and the local JSON file before alert N+1 calls `recall_by_fingerprint`. Validate each entry; on missing/empty `raw_alert` produce a placeholder `AnalysisResult` with `incident.error_type="invalid_alert"`, empty `route_trace`, and a single `AuditTraceEntry` with `route_reason="batch entry rejected: <reason>"`. (R2.1, R2.4, R2.5, design §Components, §Error Handling "Malformed batch input")
  - [x] 10.6 Honor `OPENRECALL_BATCH_SIZE_LIMIT` (default `200`) by trimming oversize batches to the limit and surfacing a Streamlit warning toast — this requires only the workflow to expose the cap; UI integration follows in task 13. (R2.1, design §Configuration / Environment Variables)

- [-] 11. Seed data migration — `data/seed_incidents.json` and memory readers
  - [x] 11.1 Extend every record in `data/seed_incidents.json` with `"triage_decision": "real"` and `"dead_ends": []` (default). The existing keys remain untouched so existing readers stay safe. (R8.6, R14.5, design §Migration Plan)
  - [x] 11.2 Update `_load_seed_memory` in `memory.py` to copy `triage_decision` and `dead_ends` from the seed record into the returned memory's `metadata` dict so `recall_by_fingerprint` and `recall_by_decision` see them. (R7.5, R8.6, design §Migration Plan)
  - [x] 11.3 Confirm `format_seed_memory` is unchanged (it does not read the new fields, so its output stays identical). Add a one-line code comment noting that the seed format is intentionally stable. (design §Migration Plan)

- [x] 12. Synthetic seed_alerts.json generator — `scripts/generate_seed_alerts.py`
  - [x] 12.1 Create `scripts/generate_seed_alerts.py` that calls `random.seed(20260101)` and emits ~100 alerts across the 8 fingerprint families that align with `data/seed_incidents.json` (`auth-service ConfigMap missing key`, `checkout-service CrashLoopBackOff env var`, `payments-api DB pool exhausted`, `worker-service Redis timeout`, `frontend feature flag 5xx`, `api-gateway JWT validation`, `waf SQL injection`, `notifications-service expired SMTP credential`). (R2.6, design §Synthetic seed_alerts.json Generator Design)
  - [x] 12.2 Implement the 50/30/15/5 distribution per design table: 50 repeats from the 8 families with surface-level wording variation, 30 one-off false positives (pen-test scanner / scheduled job / internal vuln scan), 15 novel real incidents with unique error_class + dependency_pattern combos, 5 ambiguous mixed-signal entries. Each entry must conform to the existing `data/sample_alerts.json` shape `{"title": str, "raw_alert": str}`. (R2.6, design §Synthetic seed_alerts.json Generator Design)
  - [x] 12.3 Run the generator once and commit the resulting `data/seed_alerts.json` (indent=2 for reviewable diff). The script remains in `scripts/` so the file can be regenerated deterministically. (R2.6, design §Synthetic seed_alerts.json Generator Design)
  - [x] 12.4 Checkpoint — Ensure `python -m compileall app.py incident_agent seed_memory.py scripts/smoke_test.py scripts/generate_seed_alerts.py` succeeds and the existing `make smoke` still passes against the unchanged `data/sample_alerts.json` (queue-tab smoke assertions land in task 15). Ensure all tests pass, ask the user if questions arise.

- [x] 13. Streamlit cockpit — Queue tab, override flow, cost curve, audit panels (`app.py`)
  - [x] 13.1 Introduce `st.tabs(["Single alert", "Queue"])`. Move the existing single-alert dashboard (hero, metric grid, RCA panel, audit table, learning loop) into the first tab unchanged so R1.1/R1.3 backward compatibility is preserved. The hero copy reframes from "Incident Memory Agent" to "OpenRecall" but keeps the existing CSS framework. (R1.1, R13.1, design §Backward Compatibility Plan, §Streamlit State Model)
  - [x] 13.2 Build the Queue tab with a JSON file uploader (`st.file_uploader` accepting `data/seed_alerts.json`-shaped payload) and a paste-in JSON textarea, plus an "Analyze queue" primary button that calls `workflow.analyze_queue(...)` and stores the resulting list in `st.session_state.queue_results`. (R2.2, R2.3, R13.1)
  - [x] 13.3 Render the queue table per design's "Queue UI render loop" pseudocode: one row per alert with columns `[fingerprint badge, triage badge + confidence progress bar, dead_ends panel, Override button]`. Color the triage badge per the palette (`false_positive`=green, `duplicate`=blue, `known_benign`=teal, `real`=orange, `escalated`=red). Mark novel alerts (no Strong_Match) with a "Novel — no prior memory" pill per R13.5. (R7.2, R13.1, R13.5, design §Pseudocode "Queue UI render loop")
  - [x] 13.4 Build the Override_Flow form: clicking the per-row Override button stores a draft in `st.session_state.override_drafts[idx]`. The form accepts `decision: TriageDecision`, `dead_ends: list[str]` (newline-split textarea), `analyst_id: str | None`, `business_impact_minutes: int | None`. On submit call `memory.retain(content, fingerprint=fp, decision=decision, dead_ends=dead_ends, analyst_id=analyst_id, business_impact_minutes=business_impact_minutes)` and re-run the workflow for that row to show the persisted decision. (R8.1, R8.2, R8.5, R13.2, R13.3, design §Components "Override_Flow")
  - [x] 13.5 Add a per-row collapsible audit-trace expander rendering each `AuditTraceEntry` with its model, route reason, memory_hit_count, proposed_decision, escalation_reason, live_model_call, llm_skipped, cost_usd, baseline_cost_usd, savings_usd. (R10.1, R10.2, R10.3, design §Pseudocode "Queue UI render loop")
  - [x] 13.6 Add a per-batch summary panel showing alert count, count auto-decided by memory (i.e. `triage_result.proposed_decision in {false_positive, duplicate, known_benign}` AND `requires_human_approval=True`), count escalated to strong model, total cost, total baseline, total savings, percent saved. (R9.3, R13.4, design §Components)
  - [x] 13.7 Add an Altair cost-curve chart per design §Cost Curve Chart: layered line chart (`cost_usd` vs `baseline_cost_usd`) plus shaded `mark_area` between them in green for the savings band. Use `st.altair_chart(band + chart, use_container_width=True)` with the cumulative series from `cost_tracker.series()`. (R9.2, design §Cost Curve Chart)
  - [x] 13.8 Add a top-level "Skip these paths" panel summarizing the union of `dead_ends` across the current queue, plus the per-row dead_ends expander already covered in 13.3. (R7.2, design §Components "DeadEnds Panel")
  - [x] 13.9 Cache `CostCurveTracker` as an `@st.cache_resource` so the cumulative series persists across Streamlit reruns within the same session, and reset on a new "Analyze queue" submission via `cost_tracker.begin_batch()`. (R9.1, R9.5, design §Streamlit State Model)
  - [x] 13.10 Drop the Linux-only `cd /home/edneam/...` path from the README's "How to run" section in favor of cross-platform `python -m venv .venv` / `.venv\Scripts\pip` + `.venv/bin/pip` (covered in task 16; flagged here so the UI work proceeds against a clean README).

- [x] 14. Property-based test suite (`tests/property/`)
  - [x] 14.1 Create `tests/__init__.py` and `tests/property/__init__.py` as empty package markers, and `tests/property/conftest.py` per design §Hypothesis Strategies: `_token`, `_field_or_empty`, `alert_fingerprint_strategy`, `triage_decision_strategy`, `memory_match_strategy(score_min, score_max, decision)`, `strong_match_set_strategy(dominant_decision, n_total, agreement_count)`, `raw_alert_strategy`. Register the `ci` Hypothesis profile with `seed=20260101` and `max_examples=100`. Add `frozen_router` and `frozen_memory` fixtures. (R15.1, R15.3, design §Hypothesis Strategies, §Property Test Configuration)
  - [x]* 14.2 Property test for **P1 (Fingerprint determinism in Demo_Mode)** — `tests/property/test_fingerprint.py::test_fingerprint_determinism_demo_mode` using `raw_alert_strategy` from conftest. Validates R3.3, R3.4. Per design §Test Files, lives in `test_fingerprint.py`.
  - [x]* 14.3 Property test for **P2 (Fingerprint round-trip)** — `tests/property/test_fingerprint.py::test_fingerprint_round_trip` using `alert_fingerprint_strategy`. Asserts `parse_fingerprint(format_fingerprint(fp)) == fp`. Validates R3.6, R3.7. Per design §Test Files.
  - [x]* 14.4 Property test for **P3 (Triage proposal determinism)** — `tests/property/test_triage.py::test_triage_proposal_determinism` using `alert_fingerprint_strategy` + `st.lists(memory_match_strategy())`. Two consecutive triage calls with frozen inputs must return equal `proposed_decision`/`triage_confidence`/`requires_human_approval`. Validates R5.1, R5.2, R5.3. Per design §Test Files.
  - [x]* 14.5 Property test for **P4 (Triage confidence bounds)** — `tests/property/test_triage.py::test_triage_confidence_bounds` using `alert_fingerprint_strategy` + arbitrary match lists. Asserts `0.0 <= triage_confidence <= 1.0`. Validates R5.2.
  - [x]* 14.6 Property test for **P5 (Confidence reflects history)** — `tests/property/test_triage.py::test_confidence_reflects_history` using `strong_match_set_strategy(dominant_decision, n_total, agreement_count)`. Asserts `c * (n / (n + 1)) <= triage_confidence <= c`. Validates R5.2, R5.5.
  - [x]* 14.7 Property test for **P6 (Strong-model bypass invariant)** — `tests/property/test_triage.py::test_strong_model_bypass_invariant` constructing fingerprints with empty `attack_pattern` plus consistent Strong_Matches via `strong_match_set_strategy(decision_in={false_positive, duplicate, known_benign}, agreement_count=n_total)` then running the full `IncidentWorkflow.analyze` and asserting zero `RouteTrace` entries with `model == "openai/gpt-oss-120b"` AND `live_model_call == True` AND exactly one entry with `model == "memory-bypass"` AND `llm_skipped == True`. Validates R6.1, R6.4, R6.5. Per design §Test Files.
  - [x]* 14.8 Property test for **P11 (No-Strong-Match implies escalation)** — `tests/property/test_triage.py::test_no_strong_match_escalates` using match lists where every score is `< 0.85`. Asserts `proposed_decision == "escalated"`. Validates R5.4.
  - [x]* 14.9 Property test for **P12 (Security-novel bypass refusal)** — `tests/property/test_triage.py::test_security_novel_escalates` using `alert_fingerprint_strategy` filtered to non-empty `attack_pattern` + match lists with no Strong_Match. Asserts `proposed_decision == "escalated"`. Validates R5.6.
  - [x]* 14.10 Property test for **P7 (Idempotent retain)** — `tests/property/test_memory.py::test_retain_idempotent` using `alert_fingerprint_strategy`, `triage_decision_strategy`, `st.text` for content, `st.lists(st.text())` for dead_ends. Calls `retain` twice and asserts the underlying store has exactly one record matching the dedupe key. Uses `frozen_memory` fixture. Validates R8.3. Per design §Test Files.
  - [x]* 14.11 Property test for **P10 (Fallback-vs-cloud structural parity)** — `tests/property/test_memory.py::test_fallback_vs_cloud_structural_parity` using `raw_alert_strategy`. Compares `AnalysisResult` Pydantic class, field names, and nested model classes per field across the Hindsight-mocked path and the Local_Fallback_Store path; value equality is not required. Validates R11.4. Per design §Test Files.
  - [x]* 14.12 Property test for **P13 (Recall-then-retain consistency)** — `tests/property/test_memory.py::test_recall_then_retain_consistency` using `alert_fingerprint_strategy` + `triage_decision_strategy` + `st.lists(st.text())` for dead_ends. Asserts that after a successful retain, the next `recall_by_fingerprint(fp)` returns at least one MemoryMatch whose `metadata.triage_decision == decision` AND whose `metadata.dead_ends` contains every retained dead end. Validates R7.4, R7.5, R8.5.
  - [x]* 14.13 Property test for **P8 (Cost-curve monotonicity under repeating distribution)** — `tests/property/test_cost_curve.py::test_cost_curve_monotonic_repeating_distribution` constructing two batches `B1, B2` iid from a small fixture distribution, processing `B1` then retaining, then processing `B2`. Asserts `cost_tracker.mean_cost_per_alert(batch_id=2) <= cost_tracker.mean_cost_per_alert(batch_id=1)`. Validates R9.5.
  - [x]* 14.14 Property test for **P9 (Cost-curve never exceeds baseline)** — `tests/property/test_cost_curve.py::test_cost_curve_never_exceeds_baseline` using `st.lists(st.tuples(st.floats(min_value=0.0), st.floats(min_value=0.0)).filter(lambda x: x[0] <= x[1]))`. Asserts every `CostCurvePoint.cost_usd <= baseline_cost_usd` and that the series is sorted by `alert_index` with no gaps. Validates R9.1, R9.4.
  - [x]* 14.15 Property test for **P14 (Budget enforcement)** — `tests/property/test_cost_curve.py::test_budget_enforcement` using `st.floats(min_value=0.001, max_value=0.05)` for `CASCADEFLOW_RUN_BUDGET_USD` and `st.lists(...)` for synthetic alerts. Asserts `sum(t.estimated_cost_usd for t in route_trace if t.live_model_call) <= budget + estimated_cost_of_final_in_flight_call` and that every RouteTrace after the budget hit carries `budget_exhausted=True` AND `live_model_call=False`. Validates R6.6.
  - [x]* 14.16 Property test for **P15 (Audit trace completeness)** — `tests/property/test_workflow.py::test_audit_trace_completeness` using `raw_alert_strategy`. For every analyzed alert asserts `len(audit_trace) == len(route_trace)` AND every `AuditTraceEntry` has non-None `step`, `model`, `route_reason`, `live_model_call`, `cost_usd`, `baseline_cost_usd`. Validates R10.1, R10.3.
  - [x]* 14.17 Property test for **queue ordering** — `tests/property/test_workflow.py::test_analyze_queue_preserves_order` using `st.lists(st.builds(Alert, raw_alert=raw_alert_strategy), min_size=1, max_size=20)`. Asserts the returned list length equals the input length and that `result[i].incident.raw_alert == alerts[i].raw_alert` for every `i`. Validates R2.1, R2.4. Per design §Test Files.
  - [x]* 14.18 Property test for **Demo_Mode end-to-end** — `tests/property/test_workflow.py::test_demo_mode_end_to_end` using `raw_alert_strategy` with `frozen_router` (LIVE_GROQ off) and `frozen_memory` (Hindsight unreachable, local fallback). Asserts every analyze result has a populated `alert_fingerprint`, `triage_result`, `cost_curve_point`, `audit_trace`, AND that no `RouteTrace.live_model_call` is True. Validates R3.3, R11.6, R14.3.

- [x] 15. Smoke test extensions, Make targets, CI
  - [x] 15.1 Extend `scripts/smoke_test.py` per design §Example/Smoke Tests: load `data/seed_alerts.json`, run `workflow.analyze_queue`, assert at least one `RouteTrace` has `step == "auto-triage bypass"`, assert cumulative `cost_usd <= cumulative baseline`, assert at least one returned `AnalysisResult.dead_ends` is non-empty. Keep the existing single-alert assertions intact so smoke also covers the backward-compat path. (R14.3, design §Example/Smoke Tests)
  - [x] 15.2 Add an `.env.example` content check to the smoke test: open the file, assert presence of every required key from the design Configuration table, AND assert the regex `gsk_[A-Za-z0-9_]{20,}|hsk_[A-Za-z0-9_]{20,}` does NOT match. (R12.4, design §Example/Smoke Tests)
  - [x] 15.3 Add a `data/seed_incidents.json` schema check to the smoke test: every record exposes `triage_decision` and `dead_ends`. (R8.6, design §Example/Smoke Tests)
  - [x] 15.4 Update `Makefile`: add `pbt: ; .venv/bin/python -m pytest tests/property -q`. Extend `compile` to include `incident_agent/fingerprint.py`, `incident_agent/triage.py`, `incident_agent/cost_curve.py`, `incident_agent/audit.py`, `scripts/generate_seed_alerts.py`. Update `smoke` to depend on `pbt` (`smoke: compile pbt`). Mark all new targets `.PHONY`. (R14.4, design §File-by-file Plan)
  - [x] 15.5 Update `.github/workflows/ci.yml` per design §File-by-file Plan: add `OPENRECALL_PBT_SEED=20260101` to the env block; add a `python -m pytest tests/property --hypothesis-profile=ci` step before the existing smoke step; keep `HINDSIGHT_BASE_URL=http://127.0.0.1:9` so PBT and smoke run against the local fallback path. (R14.3, R15.3, design §File-by-file Plan)
  - [x] 15.6 Checkpoint — run `make compile`, `make pbt`, `make smoke` locally; confirm all green. Ensure all tests pass, ask the user if questions arise.

- [x] 16. Docs reframe and content
  - [x] 16.1 Reframe `README.md` top section as "OpenRecall — alert triage co-pilot for SOC analysts and on-call SRE/devs": three-pillar pitch (counterfactual memory, cost curve visibility, alert DNA fingerprinting), demo flow (load `data/seed_alerts.json`, watch the cost curve drop, override one row, retain, watch the next batch bypass it), and judging-rubric mapping. Keep the existing setup/run sections; replace the Linux-only `cd /home/edneam/...` path with cross-platform `python -m venv .venv` instructions. (R14.5, design §File-by-file Plan)
  - [x] 16.2 Rewrite `docs/ARCHITECTURE.md` with the queue-driven flow per design §Architecture; embed the four Mermaid diagrams from `design.md` (System Context, Component, Primary Sequence, Secondary Sequence — Retain). Keep the Data Boundaries section; expand it to mention the `dead_ends` and `triage_decision` metadata fields. (design §File-by-file Plan)
  - [x] 16.3 Rewrite `docs/DEMO.md` with the 60-second demo script: load `data/seed_alerts.json`, watch the cost curve drop between batch 1 and batch 2, override one row, retain, re-run analyze for that row to show the new memory hit, then re-process batch 2 to show the bypass kick in. (design §File-by-file Plan)
  - [x] 16.4 Update `docs/HACKATHON_SUBMISSION.md` to map each rubric category to one of the three pillars (counterfactual memory + auto-triage, cost curve visibility, alert DNA fingerprinting) and add a "Technical Implementation" bullet calling out 15 Hypothesis property tests covering 15 correctness properties. (R15, design §File-by-file Plan)
  - [x] 16.5 Reframe `content/article_draft.md`, `content/social_post.md`, `content/demo_video_script.md` from "Incident Memory Agent" to "OpenRecall" preserving the voice. Update screenshots/captions where they reference the old hero. (design §File-by-file Plan)

- [x] 17. End-to-end verification
  - [x] 17.1 Run `make compile` followed by `make pbt` followed by `make smoke` against fallback (`HINDSIGHT_BASE_URL=http://127.0.0.1:9`, `CASCADEFLOW_LIVE_GROQ=false`). Confirm zero failures. (R14.1, R14.3, R14.4, R15.2)
  - [x] 17.2 Run `streamlit run app.py` against the four-quadrant Mode Matrix from design §Mode Matrix: (a) Hindsight Cloud + Demo_Mode, (b) Hindsight Cloud + Live Groq, (c) Local Fallback + Live Groq, (d) Local Fallback + Demo_Mode. Verify the badge row reflects each mode and the Queue tab functions in all four. (R11.4, R11.6, R12.5, design §Mode Matrix)
  - [x] 17.3 Load `data/seed_alerts.json` through the Queue tab in two passes (batch 1, then batch 2 after retaining one override decision). Confirm the cost curve drops visibly between batch 1 and batch 2. (R9.2, R9.5, design §Synthetic seed_alerts.json Generator Design)
  - [x] 17.4 Confirm at least one alert in batch 2 auto-bypasses without firing a strong-model live call (RouteTrace contains `model="memory-bypass"`, `llm_skipped=True`, `live_model_call=False` AND zero entries with `model="openai/gpt-oss-120b"` AND `live_model_call=True`). (R6.1, R6.4, P6)
  - [x] 17.5 Confirm the `dead_ends` panel renders the union of dead ends from batch-2 matches when the corresponding override flow recorded any. (R7.1, R7.2, R7.5)
  - [x] 17.6 Final checkpoint — Ensure all tests pass and the demo runs end to end. Ask the user if questions arise.

## Notes

- Tasks marked with `*` are optional sub-tasks per the spec workflow convention. The PBT sub-tasks under task 14 are functionally required to satisfy R15 and the 15 correctness properties; they are flagged optional only because they are testing tasks per the workflow's "optional task marking" rule. Implement them all for hackathon submission.
- Each task references specific requirements (R#.#) and properties (P#) for traceability. All references are cross-checked against `requirements.md` and `design.md`.
- Checkpoints (12.4, 15.6, 17.6) ensure incremental validation between phases.
- Property tests validate the universal correctness invariants. Unit/example tests validate signatures and seed data shape. The smoke test exercises the queue end-to-end against the local fallback path so CI never needs a live network.
- The pre-OpenRecall single-alert path (R1) is preserved exactly: `IncidentWorkflow.analyze(raw_alert)` keeps its signature, every new `AnalysisResult` field has a safe default. Phase 2 below records how the Streamlit cockpit referenced in tasks 13 and 17 was retired without breaking R1.

---

## Phase 2 — Streamlit cutover (post-launch)

The Streamlit cockpit shipped under tasks 13 and 17 was retired in this phase. The cutover preserved every package-level contract (`incident_agent/*.py`, the 15 correctness properties, the backward-compat signature gate, the smoke test) and replaced only the entry point and the analyst-facing UI.

- [x] 18. Streamlit cutover to FastAPI + Next.js
  - [x] 18.1 Delete the Streamlit runtime: `app.py`, `app.py.streamlit.bak`, `.streamlit/` config dir, `streamlit.log`, and `__pycache__/app.cpython-*.pyc`. (R14.5; supersedes task 13)
  - [x] 18.2 Update `pyproject.toml` and `requirements.txt`: drop `streamlit` and `altair`; add `fastapi>=0.136.0`, `uvicorn>=0.47.0`. Dev deps remain `hypothesis>=6.108`, `pytest>=8.0`. (R14.4; supersedes task 2.2, 2.3)
  - [x] 18.3 Replace `Makefile` `run` target with `api` (`uvicorn api:app --reload --port 8000`) and `frontend` (`cd frontend && npm run dev`). Update `compile` to compile `api.py` instead of `app.py`. (R14.2)
  - [x] 18.4 Update `.gitignore`: drop `.streamlit/secrets.toml`, add `data/decision_cache.json`, `frontend/.next/`, `frontend/node_modules/`, `frontend/out/`. (R12.2; supersedes task 1.2)
  - [x] 18.5 Drop Streamlit references from `incident_agent/cost_curve.py` and `incident_agent/workflow.py` docstrings: replace `@st.cache_resource` mentions with module-level singleton language and replace "Streamlit session" with "FastAPI process". (R1.2 — backward compat preserved; supersedes task 6.1, 10.1)
  - [x] 18.6 Add `api.py` at the repo root: FastAPI app with module-level `IncidentMemory`, `CascadeFlowRouter`, `CostCurveTracker`, `IncidentWorkflow` singletons; `lifespan` auto-seeds Hindsight; `CORSMiddleware` open in dev; routes `/health`, `/stats`, `/cost-curve`, `/seed`, `/analyze`, `/retain`. (R13.x — Cockpit + Override flow contract; supersedes task 13)
  - [x] 18.7 Add `frontend/`: Next.js 16 App Router project with Tailwind v4, shadcn 4, and `frontend/src/components/ui/v0-ai-chat.tsx` as the cockpit. Renders streaming agent steps, decision pill, fingerprint section, prior incidents, dead ends, cost line, audit panel, and the inline override form (which calls `POST /retain`). (R13.1, R13.2, R13.3, R13.5, R13.6; supersedes task 13)
  - [x] 18.8 Rewrite README, HANDOVER, CONTRIBUTING, SECURITY: replace every `streamlit run app.py` with `uvicorn api:app --reload --port 8000` plus `cd frontend && npm run dev`; replace `.streamlit/secrets.toml` with `data/decision_cache.json` in sensitive-files lists. (R14.5; supersedes task 16.1)
  - [x] 18.9 Rewrite `docs/ARCHITECTURE.md`: replace the Streamlit cockpit component with the Next.js + FastAPI split; update the four Mermaid diagrams. Rewrite `docs/DEMO.md` as a 60-second runbook over uvicorn + npm run dev. Update `docs/HACKATHON_SUBMISSION.md` UX + Evaluation sections. Rewrite `content/social_post.md` and `content/demo_video_script.md`. (R14.5; supersedes task 16.2, 16.3, 16.4, 16.5)
  - [x] 18.10 Replace `.kiro/steering/streamlit-cockpit.md` with `.kiro/steering/nextjs-cockpit.md` (fileMatch `frontend/**/*.tsx`, `frontend/**/*.ts`, `api.py`). Update `.kiro/steering/rules.md` (RULE-CODE-07 module-level singleton; RULE-COST-03 references `/cost-curve`; RULE-SEC-02 swaps `.streamlit/secrets.toml` → `data/decision_cache.json`; RULE-TEST-04 references `api.py` + `frontend/`; section 9 renamed to "Next.js cockpit" with RULE-UI-01..06). Update `tech.md`, `structure.md`, `product.md`, `python-conventions.md` for the new stack. Update all `.kiro/skills/*` and `.kiro/hooks/*`. (RULE-KIRO-04)
  - [x] 18.11 Update `requirements.md`: rename `Streamlit_Cockpit` glossary term → `Cockpit`; replace `app.py` references with `api.py` + `frontend/`; update R12.2 gitignore list and R13 acceptance criteria. Update `design.md`: replace Streamlit_Cockpit and Cost Curve Chart sections with Next.js + `/cost-curve` API description; update mermaid diagrams; update Component Responsibilities Table; replace Streamlit State Model with Cockpit and Backend State Model. (R14.5; supersedes task 16.2)
  - [x] 18.12 Final verification: `make compile && make pbt && make smoke && python -c "import api; print(len(api.app.routes))"`. Confirm 17 PBTs pass under `OPENRECALL_PBT_SEED=20260101`, signature tests pass, all six FastAPI routes register. (R14.1, R14.3, R14.4)
